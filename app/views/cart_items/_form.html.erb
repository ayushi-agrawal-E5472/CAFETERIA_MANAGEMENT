<%= form_with(model: cart_item) do |form| %>
    <% if cart_item.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(cart_item.errors.count, "error") %> prohibited this cart_item from being saved:</h2>
  
        <ul>
          <% cart_item.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :menu_item_id %>
      <%= form.text_field :menu_item_id %>
    </div>
  
    <div class="field">
      <%= form.label :menu_item_name %>
      <%= form.text_field :menu_item_name %>
    </div>
  
    <div class="field">
      <%= form.label :menu_item_price %>
      <%= form.text_field :menu_item_price %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>